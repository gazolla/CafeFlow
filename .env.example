# ============================================================
# CafeFlow Environment Configuration
# ============================================================
# Copy this file to .env and fill in the values you need.
# Only configure the variables for the helpers your workflow uses.
#
#   cp .env.example .env
#   # Edit .env with your values
#   mvn spring-boot:run
#
# The ConfigurationValidator will show which helpers are ready
# and which are missing configuration on startup.
# ============================================================

# === LLM / AI Helpers ===
# Required for: TextSummarizerHelper, SentimentAnalyzerHelper,
#   TextTranslatorHelper, ContentGeneratorHelper, DataExtractorHelper,
#   TextClassifierHelper, TopicExtractorHelper
# At least one provider (Gemini or Groq) must be configured.
# LLM_DEFAULT_PROVIDER=gemini
# GEMINI_API_KEY=
# GEMINI_MODEL=gemini-1.5-flash
# GROQ_API_KEY=
# GROQ_MODEL=llama-3.3-70b-versatile

# === Email (SMTP) ===
# Required for: EmailHelper
# For Gmail: enable 2FA, then create an App Password
#   Google Account > Security > App Passwords > Mail
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=
# SMTP_PASSWORD=

# === Telegram ===
# Required for: TelegramHelper
# Create a bot via @BotFather on Telegram to get the token
# TELEGRAM_BOT_TOKEN=

# === Twitter / X ===
# Required for: TwitterHelper
# Get credentials from https://developer.twitter.com
# X_BEARER_TOKEN=
# X_API_KEY=
# X_API_SECRET=

# === Google Drive ===
# Required for: GDriveHelper
# Download credentials.json from Google Cloud Console
# GD_APP_NAME=CafeFlow
# GD_CREDENTIALS_PATH=/credentials.json
# GD_TOKENS_DIR=tokens

# === Slack ===
# Required for: SlackHelper
# Create an Incoming Webhook at https://api.slack.com/messaging/webhooks
# SLACK_WEBHOOK_URL=

# === GitHub ===
# Required for: GitHubHelper
# Create a Personal Access Token at https://github.com/settings/tokens
# GITHUB_TOKEN=
